---
# vars file for ansible-wifi_ap

# Auxilary variables which don't affect the configuration,
# but only affect the role execution
wifi_ap_dhcp_restart: {failed: no}
wifi_ap_iptables_install: {changed: no}
...
